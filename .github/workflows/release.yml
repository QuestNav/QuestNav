name: Publish Release
on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version string to use (optional, auto-generated if not provided)'
        required: false
        type: string
concurrency:
  group: publish-release
  cancel-in-progress: false

jobs:
  build-apk:
    uses: ./.github/workflows/reusable-build-csharp.yml
    with:
      version: ${{ inputs.version }}
    secrets:
      UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
      UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
      UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}

  build-lib:
    uses: ./.github/workflows/reusable-build-java.yml
    with:
      version: ${{ inputs.version }}

  publish-docs:
    uses: ./.github/workflows/reusable-publish-docs.yml
    with:
      version: ${{ inputs.version }}
  
  publish-java:
    uses: ./.github/workflows/reusable-publish-java.yml
    with:
      version: ${{ inputs.version }}

  github-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    needs: [build-apk, build-lib, publish-docs, publish-java]
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Determine Version
        id: version-info
        uses: ./.github/actions/determine-version
        with:
          version: ${{ inputs.version }}

      - name: Download APK
        uses: actions/download-artifact@v4
        with:
          name: 'questnav-apk-${{ steps.version-info.outputs.version }}'
          path: 'release'

      - name: Download Vendor JSON
        uses: actions/download-artifact@v4
        with:
          name: 'questnav-lib-${{ steps.version-info.outputs.version }}'
          path: 'release'

      - name: Release to GitHub Releases
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          fail_on_unmatched_files: true
          tag_name: ${{ steps.version-info.outputs.tag }}
          files: release/**
          draft: true
          token: ${{ secrets.GITHUB_TOKEN }}